version: '3.8'

services:
  work-tool:
    build: .
    ports:
      - "8000:8000"  # HTTP服务器端口
      - "8765:8765"  # WebSocket服务器端口
    volumes:
      - ./config:/app/config
      - ./static:/app/static
    environment:
      - HOST_IP=0.0.0.0