version: '3.8'

services:
  work-tool:
    build: .
    ports:
      - "8020:8020"  # HTTP服务器端口
      - "8021:8021"  # WebSocket服务器端口
    volumes:
      - ./config:/app/config
      - ./static:/app/static
    environment:
      - HOST_IP=0.0.0.0